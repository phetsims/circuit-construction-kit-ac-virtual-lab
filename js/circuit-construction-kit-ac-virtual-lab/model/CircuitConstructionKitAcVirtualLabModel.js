// Copyright 2021, University of Colorado Boulder

/**
 * @author Sam Reid (PhET Interactive Simulations)
 */

import Tandem from '../../../../tandem/js/Tandem.js';
import circuitConstructionKitAcVirtualLab from '../../circuitConstructionKitAcVirtualLab.js';

class CircuitConstructionKitAcVirtualLabModel {

  /**
   * @param {Tandem} tandem
   */
  constructor( tandem ) {
    assert && assert( tandem instanceof Tandem, 'invalid tandem' );
    //TODO
  }

  /**
   * Resets the model.
   * @public
   */
  reset() {
    //TODO
  }

  /**
   * Steps the model.
   * @param {number} dt - time step, in seconds
   * @public
   */
  step( dt ) {
    //TODO
  }
}

circuitConstructionKitAcVirtualLab.register( 'CircuitConstructionKitAcVirtualLabModel', CircuitConstructionKitAcVirtualLabModel );
export default CircuitConstructionKitAcVirtualLabModel;